.section .text, "ax"
_start:
    mov r0, r0
before:
    mov r0, r0
    mov r0, r0
    mov r0, r0
    ldr r0, before
    ldr r0, after
    mov r0, r0
    mov r0, r0
    mov r0, r0
after:
    mov r0, r0
